name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Install dependencies
        working-directory: remix # 프로젝트 디렉터리
        run: |
          npm install

      - name: Build Project
        working-directory: remix # 프로젝트 디렉터리
        run: |
          npm run build

      - name: Deploy to GitHub Pages
        working-directory: remix # 프로젝트 디렉터리
        run: |
          npx gh-pages -d build --repo https://github.com/${{ github.repository }}.git
